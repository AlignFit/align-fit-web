# Endpoint POST /upload — retorna { upload_url, file_id, object_key }
VITE_UPLOAD_API_URL=

# Endpoint base GET /result — o file_id é concatenado em runtime: ${VITE_RESULT_API_URL}/${fileId}
VITE_RESULT_API_URL=
